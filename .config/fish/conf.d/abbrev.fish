function last_history_item
    echo $history[1]
end
abbr -a !! --position anywhere --function last_history_item

abbr -a vi nvim
abbr -a vim nvim
abbr -a ll "eza -la --header --icons --no-user --git $argv"
abbr -a cat bat
abbr -a cd z
